{% extends '::layout.html.twig' %}

{% block content %}

    {% set runtimeConfig = {"thumbnail": {"size": [width, height] }} %}

    {%- set imagePath -%}
        {% path image, 'reference' %}
    {%- endset -%}

    <img src="{{ imagePath | imagine_filter('dynamic', runtimeConfig) }}"/>

{% endblock %}
